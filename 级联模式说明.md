# 级联模式说明 —— "展开上游" vs "force 级联网"

> 适用版本：`--cascade-mode` 参数 / GUI「级联」下拉框引入之后。
> 背景实证：2026-06-02 `d_ndiv_n` 仿真 T6-T9 失败的调试结论。

---

## 1. 先认识 BT_LP_DREG 的内部结构

你们的 Excel 自动生成的数字模块 BT_LP_DREG 里面有**两个子模块**：

```
                          BT_LP_DREG
            ┌──────────────────────────────────────────────┐
   软件总线  │  ┌────────────┐  *_to_logic   ┌────────────┐  │
 ──────────►│  │  regfile   │ ────────────► │ sig_logic  │──┼──► *_ls / 原名
 (RF_WRITE) │  │ (寄存器堆)  │   一堆接口线   │ (组合逻辑)  │  │    输出端口给模拟
            │  └────────────┘               └────────────┘  │
  模拟来的   │        ▲                                      │
  信号(端口)─┼────────┘                                      │
            └──────────────────────────────────────────────┘
```

| 名词 | 含义 |
|---|---|
| **regfile（寄存器堆）** | regmap / total_memory_map 页生成的模块。软件按地址读写的那堆寄存器：RW 寄存器存软件写的值；RO 寄存器把模拟信号采样进来给软件读 |
| **sig_logic** | **logic 页**生成的模块。logic 页每一行 = 它里面的一条 `assign` 语句 |
| **`*_to_logic` 线** | 字面意思"送往 logic 的"。绝大多数是 regfile 的输出 → sig_logic 的输入 |
| **自引用** | 一个 logic 行的**输入列里出现了它自己 K 列的名字**（如 d2a_cnt_sclk 行的输入 A = d2a_cnt_sclk_to_logic） |

---

## 2. 问题：级联时 `X_to_logic` 到底是谁驱动的？

当一个 logic 行的输入引用了**另一个 logic 行的输出**（级联），有两种完全不同的物理情况，
**判据 = 上游那一行是否自引用**：

### 情况①：上游自引用（d2a_cnt_sclk 形态）→ `_to_logic` 是 regfile 给的

```
端口 d2a_cnt_sclk ──► regfile ──► d2a_cnt_sclk_to_logic ──► sig_logic ──► d2a_cnt_sclk_ls
     (源头在外面)                      (regfile 导出)         (iddq 门控)      (输出)
```

下游引用它时 **force 基名就有效**（值从源头流下来）。工具自动处理，与级联模式无关。

### 情况②：上游不自引用（d_wl_wur_bt_pll_mode_sel 形态）→ `_to_logic` 是 sig_logic 自己算的

```
                          sig_logic 内部
  ┌────────────────────────────────────────────────────────────────┐
  │ assign mode_sel_to_logic = (line_ctrl ? wurx : bt_mode) & ~iddq │ ← 源头是这条表达式
  │ assign mode_sel    = mode_sel_to_logic                          │ ← 基名只是【下游拷贝】
  │ assign mode_sel_ls = mode_sel_to_logic                          │ ← _ls 也只是【下游拷贝】
  │ assign d_ndiv_n_ls = mode_sel_to_logic ? {5'b0,ndiv_ls} : ...   │ ← d_ndiv_n 读的是【源头】
  └────────────────────────────────────────────────────────────────┘
```

⚠ **force 基名 / `_ls` 都没用**——那是堵下游水渠，源头照常流向 d_ndiv_n。
（仿真实证：d_ndiv_n T6-T9 失败，sim out 永远等于 mux 另一边的值）

**本文档讲的两种"级联模式"，就是针对情况②的两种驱动办法。**

---

## 3. 两种级联模式

以 d_ndiv_n 为例：`d_ndiv_n_ls = A ? {5'b0, C} : B`，其中 A = mode_sel_to_logic（情况②）。

### 模式 cone：「展开上游」（默认）

**不碰那根线，把上游的表达式代入，改为驱动上游的源头寄存器/管脚。**

```
   要让 A = 1：
                                 ┌─ RF_WRITE  line_ctrl = 0   ┐
   A = (line_ctrl ? wurx         ├─ RF_WRITE  bt_mode  = 1    ├─► 表达式算出 A = 1
        : bt_mode) & ~iddq  ◄────┴─ force     iddq     = 0    ┘
```

生成的 .sv（驱动的全是源头寄存器，级联信号本身完全不出现）：

```systemverilog
`RF_WRITE(10'h17,16'h2AAA);        // d_ndiv_n + line_ctrl (同地址合并)
`RF_WRITE(10'h2C,16'h2);           // wurx
force `ENV_RF.d_bt_lp_bt_mode_sel=16'h1;
force `ENV_RF.d_bt_lp_pll_dig_dft_iddq_mode=16'h0;
force `ENV_RF.ndiv_ls[8:0]=16'h155;
#1ps;
assert (`ENV_RF.d_ndiv_n_ls[13:0]==14'b00000101010101)
```

### 模式 force：「force 级联网」

**直接 force 那根 `_to_logic` 线**（force 优先级高于 assign，能钉住它）。

```
   sig_logic 内部
   ┌──────────────────────────────────────────────┐
   │ assign mode_sel_to_logic = (...)  ← force 钉住 │ ◄══ force `ENV_RF.<层级>.mode_sel_to_logic = 1
   │ assign d_ndiv_n_ls = mode_sel_to_logic ? ...   │     (该线在 sig_logic 模块内部，
   └──────────────────────────────────────────────┘      需要 scan_rtl 找到层级前缀)
```

生成的 .sv：

```systemverilog
`RF_WRITE(10'h17,16'h2AAA);        // d_ndiv_n
force `ENV_RF.U_BT_LP_PLL_DIG.U_BT_LP_DREG.U_SIG_LOGIC.d_wl_wur_bt_pll_mode_sel_to_logic=16'h1;
force `ENV_RF.ndiv_ls[8:0]=16'h155;
#1ps;
assert (`ENV_RF.d_ndiv_n_ls[13:0]==14'b00000101010101)
```

---

## 4. 怎么选

| | **cone 展开上游**（默认） | **force 级联网** |
|---|---|---|
| 需要 scan_rtl 前缀吗 | ❌ 不需要（纯 Excel） | ✅ 需要（`_to_logic` 网在 sig_logic 模块内部） |
| 验证粒度 | 上游逻辑 + 本行逻辑**一起验**（端到端） | **只验本行**（每行 logic 隔离验证） |
| fail 时定位 | 上游有 bug 会连带本行 fail（要看两行） | 准——哪行错哪行报 |
| 测试向量数 | 多（上游的控制位也要组合） | 少（级联输入只是 1 个变量） |
| 没配前缀时 | 照常生成 | 该信号被**跳过**（给原因，保证 .sv 能 elaborate） |

**建议**：
- 日常/首轮验证用 **cone（默认）**——不依赖 scan_rtl，开箱即用，顺便把级联连接也验了。
- 某个信号 fail 了、想定位到底是上游还是本行的问题 → 切到 **force** 模式重新生成这个信号，
  隔离验证（需要先跑 scan_rtl 拿前缀）。
- 两种模式可以混用：定位问题时只对可疑信号用 force 模式单独出一份 .sv。

---

## 5. 怎么切换

**GUI**：底部选项栏「级联:」下拉框（旁边 `?` 按钮在程序内置帮助窗口中显示本文档）。切换后全表自动重新解析，
真值表/预览/导出立即按新模式生成。选择会记住，下次启动保持。

**CLI**：

```powershell
# 默认就是 cone（展开上游）
python -m dreg_verify.cli --excel 真表.xlsx --out wr_rf_tc.sv

# 切到 force 级联网模式（需要先把 scan_rtl 的 probe_prefixes.txt 配进来）
python -m dreg_verify.cli --excel 真表.xlsx --cascade-mode force `
       --probe-prefix-file probe_prefixes.txt --out wr_rf_tc.sv
```

**scan_rtl 提示**：`scan_rtl.py --export-nets` 导出的 nets.txt **同时包含两种模式需要的网**，
所以 RTL 只用扫一次，之后随意切换模式都不用重新扫。

---

## 6. 附：怎么在 Excel 里亲眼看出情况①和情况②

打开 logic 页，找到上游信号的那一行：

| 看什么 | 情况①（regfile 前级） | 情况②（上游计算网） |
|---|---|---|
| 该行的输入列（A~J） | **有它自己的名字**（自引用） | 全是别的信号 |
| Q 列 export_from_regmap 里搜 `<它>_to_logic` | **能搜到** | 搜不到 |
| 例子 | d2a_cnt_sclk、d_ndiv_n | d_wl_wur_bt_pll_mode_sel |

两个判据等价，工具用的是第一个（自引用），因为在 logic 页内部就能判断。
